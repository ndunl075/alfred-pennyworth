# Copy this file to .env and fill in a real username/password before
# running `docker compose up`. .env is gitignored; never commit real
# credentials here.
COUCHDB_USER=changeme
COUCHDB_PASSWORD=changeme
